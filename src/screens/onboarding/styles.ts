import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  content: {
    height: '85%',
    width: '100%',
  },
  buttonContainer: {
    paddingLeft: 30,
    width: 150,
  },
});

export default styles;
