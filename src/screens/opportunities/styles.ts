import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  header: {
    marginBottom: 15,
  },
  cardWrapper: {
    paddingBottom: 10,
  },
});

export default styles;
