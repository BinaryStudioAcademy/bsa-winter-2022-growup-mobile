import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const { reducer, actions } = createSlice({
  name: 'skill',
  initialState,
  reducers: {},
});

export { reducer, actions };
