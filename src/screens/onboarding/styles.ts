import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  content: {},
  inputContainer: {
    padding: 20,
  },
  buttonContainer: {
    paddingLeft: 30,
    width: 150,
  },
});

export default styles;
